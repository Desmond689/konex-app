{
  "buildCommand": "npm install --prefix konex-auth-site && npm run build --prefix konex-auth-site",
  "outputDirectory": "konex-auth-site/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
